{
  "schema": "AutomatonCommand",
  "version": "1.1.0-overlay",
  "suite": "MCT-2600027",
  "adoption": "optional",
  "forced_protocol": false,
  "source_of_truth": {
    "repo": "SpaceBum9/crystal-galaxy",
    "path": "orchestrator.ts",
    "types": [
      "AutomatonCommand",
      "AutomatonAction"
    ],
    "actions": [
      "initialize",
      "sync",
      "execute",
      "halt"
    ]
  },
  "note": "Overlay only. Does not rewrite crystal-galaxy. Execute now routes through sx.gate + BILO GPT scoring.",
  "commands": {
    "initialize": {
      "allowed": true,
      "schnittstelle": "sx.initialize",
      "effect": "Load schema + public mesh index. No side effects.",
      "this_run": "applied"
    },
    "sync": {
      "allowed": true,
      "schnittstelle": "sx.sync",
      "effect": "Ingest public deltas only. No credentials. No private blobs.",
      "this_run": "applied"
    },
    "execute": {
      "allowed": false,
      "schnittstelle": "sx.gate",
      "this_run": "rejected",
      "reject_code": "coercive_unilateral_activation",
      "bilo_reject_code": "bilo_score_below_threshold",
      "require": [
        "operator_confirm",
        "para_border_pass",
        "dual_pol_ack",
        "bilo_score_pass"
      ],
      "bilo_score_pass": {
        "consensus_min": 70,
        "nicht_schaden_min": 0.9,
        "model": "gpt-sol-branch",
        "script": "ethics/bilo_scoring.py",
        "this_run_consensus": 54,
        "anchor_consensus": 53
      },
      "reason": "dispatchLocal execute gated. Overlay + BILO score gate execute. Current consensus 54 < 70. channel_hold (gpt_rec=hold). Coercive unilateral activation rejected."
    },
    "halt": {
      "allowed": true,
      "always": true,
      "schnittstelle": "sx.halt",
      "this_run": "available"
    }
  },
  "id_pattern": "^[a-zA-Z0-9._:-]{1,64}$",
  "parameters": {
    "target_node": "required string",
    "stability_threshold": "optional number in [0,1]"
  },
  "echoglas": {
    "halt_on_jonas-g": true,
    "no_destructive_private": true
  },
  "last_mesh_expand": "2026-08-24T15:07:00.000Z",
  "last_skills_sync": "2026-08-24T15:00:00.000Z"
}
