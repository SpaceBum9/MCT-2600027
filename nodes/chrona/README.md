# Chrona · SoS Palette node

Five clocks as constituent systems. Contrast is the coupling layer. Vision (CVD) is an overlay. Tokens export as CSS custom properties and SoS JSON v3.

Stack: `MCT-RC-BILO-ATM-FRAMEWORK-SOS`. Framework: GARAS SoS v3.0. Host label SoS v4.1 is not a schema file.

| Uhr | Rolle | Token |
|-----|-------|-------|
| I | Tinte | `--sos-i` / `--sos-tinte` |
| II | Erde | `--sos-ii` / `--sos-erde` |
| III | Marke | `--sos-iii` / `--sos-marke` |
| IV | Dunst | `--sos-iv` / `--sos-dunst` |
| V | Papier | `--sos-v` / `--sos-papier` |

Semantic: `--sos-fg` / `--sos-bg` = recommended AA pair (Tinte on Papier when it holds).

HOLD. `execute: false`. No credentials. No live rail.

Kernel: `color.ts` `palette.ts` `a11y.ts` `sos.ts`
